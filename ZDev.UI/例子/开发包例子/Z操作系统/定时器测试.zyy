使用包:Z操作系统;
简略使用:控制台;
简略使用:时间日期辅助;


属性:
	整数:计数器=0;

过程:启动
	T=定时器(间隔:1000);
	T的运行内容=打印时间;
	T的停止条件=计数器>=5;
	(T)启动;
	(控制台)等待按键;

过程:打印时间
	TIME =  现在时间;
	(TIME)转化为文本=>时间文本;
	计数器=计数器+1;
	打印(时间文本+"  打印"+计数器+"次");
	(控制台)换行;
